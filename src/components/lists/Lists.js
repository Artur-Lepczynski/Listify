import style from "./Lists.module.css"; 
import Page from "../UI/Page";

export default function Lists(){
  //select or edit mode in props
  //this is the list of lists
  return <Page>
    <h1>Lists placeholder (select or edit)</h1>
  </Page>
}